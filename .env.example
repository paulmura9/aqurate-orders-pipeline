# Target database -------------------------------------------------------------
# Supabase: Project settings -> Database -> Connection string -> URI.
# Use the session pooler (port 5432) or the transaction pooler (6543) - both work.
DATABASE_URL=postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:5432/postgres

# Source endpoint (from the challenge brief) ----------------------------------
ORDERS_SOURCE_URL=https://jzozteoirwfczccltcdr.supabase.co/rest/v1/orders_raw
ORDERS_SOURCE_API_KEY=sb_publishable_Xwjiw--qkKcbMuSbKd6I2w_wN9mpNTv

# FX ---------------------------------------------------------------------------
FX_API_BASE=https://api.frankfurter.dev/v1
FX_LOOKBACK_BUFFER_DAYS=10

# Optional: dead man's switch (healthchecks.io / Better Stack / Cronitor).
# Pinged only after a fully successful run - see "Monitoring" in the README.
# HEARTBEAT_URL=https://hc-ping.com/<uuid>

# Free-text label stored in ops.pipeline_run.triggered_by
RUN_TRIGGER=manual
